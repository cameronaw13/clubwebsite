import React from "react";

const Menu: React.FC = (tabs) => {
  return (
    <div style={{ position: "fixed", color: "#fff", zIndex: "1" }}>
      THIS IS A MENU!
    </div>
  );
}

export default Menu;